<%= form_for(@detalhe) do |f| %>
  <% if @detalhe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@detalhe.errors.count, "error") %> prohibited this detalhe from being saved:</h2>

      <ul>
      <% @detalhe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.text_field :item_id %>
  </div>
  <div class="field">
    <%= f.label :quantidade %><br>
    <%= f.number_field :quantidade %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
